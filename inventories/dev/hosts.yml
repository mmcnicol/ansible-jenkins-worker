---
# Example inventory shape only. Real hostnames/IPs belong in a private inventory
# (or this file, kept out of the public repo / added after the fields below are
# genericised) — nothing environment-specific is committed here.
all:
  children:
    jenkins_workers:
      hosts:
        jenkins-worker-01.example.internal: {}
        jenkins-worker-02.example.internal: {}
        # jenkins-worker-03..06.example.internal
