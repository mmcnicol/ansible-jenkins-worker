---
- name: Apply Podman graphroot SELinux context
  ansible.builtin.command: "restorecon -Rv {{ jenkins_worker_containers_dir }}"
  changed_when: true

- name: Apply jenkins home SELinux context
  ansible.builtin.command: "restorecon -Rv {{ jenkins_worker_home }}"
  changed_when: true
